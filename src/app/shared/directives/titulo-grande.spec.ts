import { TituloGrande } from './titulo-grande';

describe('TituloGrande', () => {
  it('should create an instance', () => {
    const directive = new TituloGrande();
    expect(directive).toBeTruthy();
  });
});
